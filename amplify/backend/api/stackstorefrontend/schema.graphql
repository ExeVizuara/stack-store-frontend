input AMPLIFY {
  globalAuthRule: AuthRule = {allow: public}
}

type Products @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  category: String!
  code: Int!
  expiration: AWSDate
  stock: Int!
  cost: Int!
  discount: Int
  price: Int!
} 